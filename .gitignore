# Workspace-level lockfiles (root package-lock.json is the source of truth)
frontend/package-lock.json
backend/package-lock.json

/node_modules

.turbo

# Environment files
.env
.env.local
.env.*.local
